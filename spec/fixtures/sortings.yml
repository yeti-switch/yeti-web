# == Schema Information
#
# Table name: sortings
#
#  id                :integer(4)       not null, primary key
#  description       :string
#  name              :string
#  use_static_routes :boolean          default(FALSE), not null
#

sorting_1:
  id: 1
  name: LCR,Prio, ACD&ASR control
  description: Default dialpeer sorting method
sorting_2:
  id: 2
  name: LCR, No ACD&ASR control
  description: Without ACD&ASR control
sorting_3:
  id: 3
  name: Prio,LCR, ACD&ASR control
  description: Same as default, but priotity has more weight
sorting_4:
  id: 4
  name: LCRD, Prio, ACD&ASR control
  description: Same as default, but take in account diff between costs
